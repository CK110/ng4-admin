import { IfRequiredDirective } from './if-required.directive';

describe('IfRequiredDirective', () => {
  it('should create an instance', () => {
    // const directive = new IfRequiredDirective();
    // expect(directive).toBeTruthy();
  });
});
