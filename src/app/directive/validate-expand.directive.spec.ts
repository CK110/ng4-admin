import { ValidateExpandDirective } from './validate-expand.directive';

describe('ValidateExpandDirective', () => {
  it('should create an instance', () => {
    // const directive = new ValidateExpandDirective();
    // expect(directive).toBeTruthy();
  });
});
