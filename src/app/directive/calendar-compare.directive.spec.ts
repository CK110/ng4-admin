import { CalendarCompareDirective } from './calendar-compare.directive';

describe('CalendarCompareDirective', () => {
  it('should create an instance', () => {
    const directive = new CalendarCompareDirective();
    expect(directive).toBeTruthy();
  });
});
