import { CalendarRangeDirective } from './calendar-range.directive';

describe('CalendarRangeDirective', () => {
  it('should create an instance', () => {
    const directive = new CalendarRangeDirective('');
    expect(directive).toBeTruthy();
  });
});
